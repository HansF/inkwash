# dependencies
node_modules/

# generated Capacitor web bundle
www/

# native platform projects: android/ is committed (it carries the themed status
# bar + window background); ios/ stays generated. android/.gitignore excludes its
# own build output and the copied web assets.
/ios/

# misc
.DS_Store
*.log
